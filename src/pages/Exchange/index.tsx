import React, { FC } from 'react';
import styles from './styles.module.scss';

interface Props {
  text?: string;
}

const Exchange: FC<Props> = ({ text }) => {
  return <>Hello</>;
};
export { Exchange };
